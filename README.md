# 🎓 Artifex – Smart School Diary

Artifex – это современное веб-приложение для школьников и учителей, объединяющее электронный дневник, расписание, систему оценок, геймификацию, чаты, аналитику настроения и поддержку искусственного интеллекта. Проект построен на **FastAPI** с использованием **Jinja2** для серверного рендеринга и современного подхода к безопасности (JWT, HttpOnly cookies, CSRF, собственный WAF).

![Python](https://img.shields.io/badge/python-3.10+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.115-green)
![SQLAlchemy](https://img.shields.io/badge/SQLAlchemy-2.0-red)
![JWT](https://img.shields.io/badge/JWT-RS256-orange)
![License](https://img.shields.io/badge/license-Apache_2.0-lightgrey)

---

## ✨ Основные возможности

- 🔐 **Безопасная аутентификация** – JWT (access/refresh токены) с HttpOnly, Secure, SameSite флагами, поддержка двухфакторной аутентификации (2FA) по email.
- 📅 **Дневник и расписание** – просмотр уроков, выставление оценок (с весами), создание шаблонов расписания, автоматическая генерация уроков.
- 📊 **Статистика успеваемости** – средние баллы по предметам, фильтры, итоговые оценки за учебные периоды.
- 🎮 **Геймификация** – опыт (XP), уровни, лиги, достижения, ударный режим (streak), таблица лидеров.
- 💬 **Чаты** – личные сообщения и групповые чаты (для классов).
- 👥 **Классы (группы)** – учитель создаёт группу, ученик присоединяется по коду.
- 🤖 **AI-помощник** – объяснение домашнего задания через Groq (LLaMA), разбивка задач, советы по настроению.
- 🖼️ **Загрузка фото ДЗ** – распознавание текста (OCR) с помощью EasyOCR.
- 📺 **YouTube-интеграция** – поиск образовательных видео по теме.
- 😊 **Трекер настроения** – запись эмоций, получение персонализированных советов от AI.
- ⚙️ **Встроенный WAF** – защита от SQL-инъекций, XSS, path traversal, rate limiting и других атак.
- 🌙 **Тёмная тема** – переключение одним кликом.

---

## 🛠 Технологический стек

- **Backend**: Python 3.10+, FastAPI, SQLAlchemy, Pydantic, Jinja2
- **База данных**: SQLite (для разработки), поддержка PostgreSQL при необходимости
- **Аутентификация**: JWT (RS256), itsdangerous (для 2FA)
- **Безопасность**: собственный WAF (сигнатурный анализ), CSRF-защита, заголовки безопасности
- **AI**: Groq API (LLaMA 3.3), EasyOCR, Google YouTube API
- **Фронтенд**: HTML, CSS (с переменными под тёмную тему), JavaScript (vanilla)
- **Инфраструктура**: Uvicorn, python-dotenv, celery (опционально для фоновых задач)

---

## 🚀 Быстрый старт

### Требования
- Python 3.10 или выше
- Git
- (Опционально) API ключи для Groq, YouTube

### Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Andre-wb//Artifex
cd artifex
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` в корне проекта (см. раздел **Конфигурация**).

5. Инициализируйте базу данных:
```bash
python -c "from app.database import Base, engine; Base.metadata.create_all(bind=engine)"
```

6. Запустите сервер разработки:
```bash
uvicorn app.main:app --reload
```

7. Откройте браузер по адресу: `http://localhost:8000`

---

## ⚙️ Конфигурация

Переменные окружения (файл `.env`):

```env
# Режим работы (production/development)
ENVIRONMENT=development

# Секретный ключ (обязательно смените в production!)
SECRET_KEY=your-secret-key-here

# Настройки JWT
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=80

# Пути к RSA-ключам (если не указаны, будут сгенерированы автоматически)
PRIVATE_KEY_PATH=secrets/private.pem
PUBLIC_KEY_PATH=secrets/public.pem

# API ключи (опционально)
GROQ_API_KEY=your-groq-key
YOUTUBE_API_KEY=your-youtube-key

# SMTP для отправки писем (регистрация, 2FA)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
```

---

## 📁 Структура проекта

```
artifex/
├── app/
    ├── static/                   # Статические файлы (CSS, JS, assets)
│       ├── css/
│       ├── js/
│       └── assets/                      # Основной пакет приложения
│   ├── __init__.py
│   ├── config.py             # Конфигурация из .env
│   ├── database.py           # Подключение к БД, сессии
│   ├── models.py             # SQLAlchemy модели
│   ├── schemas.py            # Pydantic схемы
│   ├── auth.py               # JWT, аутентификация, 2FA
│   ├── security.py           # Валидация паролей
│   ├── security_utils.py     # Защита от timing-атак, CSRF
│   ├── secure_cookie.py      # Безопасные cookie
│   ├── secure_upload.py      # Загрузка файлов с проверками
│   ├── create_csrf_cookie.py # Установка CSRF-куки
│   ├── email.py              # Отправка писем
│   ├── ai_funcs.py           # Взаимодействие с Groq
│   ├── ocr.py                # Распознавание текста
│   ├── youtube_search.py     # Поиск видео на YouTube
│   ├── gamification.py       # Логика XP, уровней, достижений
│   ├── load_analyzer.py      # Анализ учебной нагрузки
│   ├── templates.py          # Jinja2 шаблоны
│   ├── utils.py              # Вспомогательные функции
│   ├── waf.py                # Web Application Firewall
│   ├── middleware.py         # CSRF, обновление токенов, логи
│   ├── routes_*.py           # Маршруты по функционалу
├── templates/                # Jinja2 шаблоны HTML
├── secrets/                  # RSA-ключи (игнорируются git)
├── main.py                   # Точка входа FastAPI
├── .env                      # Переменные окружения
├── requirements.txt          # Зависимости Python
└── README.md
```

---

## 📚 Документация API

Все API-эндпоинты описаны в коде и доступны в интерактивной документации Swagger после запуска сервера:

- Swagger UI: `http://localhost:8000/docs`
- ReDoc: `http://localhost:8000/redoc`

Основные группы эндпоинтов:
- `/auth` – регистрация, вход, 2FA, refresh
- `/diary/api` – уроки, оценки, статистика, шаблоны
- `/gamification` – профиль, достижения, лидерборд
- `/chat` – личные сообщения
- `/teacher` – управление классами
- `/user` – присоединение к группам
- `/api/mood` – трекер настроения
- `/api/ai` – помощь с ДЗ
- `/api/lessons/.../upload` – загрузка фото
- `/academic` – учебные периоды, итоговые оценки
- `/waf` – управление WAF (администратор)

---

## 🤝 Вклад в проект

Мы приветствуем любые contributions! Если вы хотите помочь:

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing`)
3. Закоммитьте изменения (`git commit -m 'Add some amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing`)
5. Откройте Pull Request

Пожалуйста, следуйте существующему стилю кода и добавляйте тесты, если это возможно.

---

## 📄 Лицензия

Проект распространяется под лицензией Apache 2.0. Подробнее см. файл [LICENSE](LICENSE).

**Artifex** – сделаем учёбу увлекательной и безопасной! 🚀
